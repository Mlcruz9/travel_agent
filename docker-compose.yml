version: '3.8'

services:
  streamlit-app:
    build: .
    networks:
      - proxy
    environment:
      VIRTUAL_HOST: travelagent.miguellacruz.es, www.travelagent.miguellacruz.es
      LETSENCRYPT_HOST: travelagent.miguellacruz.es, www.travelagent.miguellacruz.es
      LETSENCRYPT_EMAIL: ${LETSENCRYPT_EMAIL}

networks:
  proxy:
    name: nginx-proxy
    external: true
